@model IEnumerable<MYVCApp.Models.Familymemberlocation>

@{
    ViewData["Title"] = "Index";
}

<h1>FamilyMemberLocations - List</h1>

<partial name="TempDataPartial"></partial>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FamilyMemberIdFk)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LocationIdFk)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.StartDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EndDate)
            </th>
            <th>
                Action
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FamilyMemberIdFk)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LocationIdFk)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.StartDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EndDate)
            </td>
            <td>
                <a asp-controller="Familymemberlocations" asp-action="details" asp-route-location="@item.LocationIdFk" asp-route-familymember="@item.FamilyMemberIdFk" asp-route-startdate="@item.StartDate">Details</a>
                <a asp-controller="Familymemberlocations" asp-action="edit" asp-route-location="@item.LocationIdFk" asp-route-familymember="@item.FamilyMemberIdFk" asp-route-startdate="@item.StartDate">Edit</a>
                <a asp-controller="Familymemberlocations" asp-action="delete" asp-route-location="@item.LocationIdFk" asp-route-familymember="@item.FamilyMemberIdFk" asp-route-startdate="@item.StartDate">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
